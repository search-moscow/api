type Product {
    _id: ID!
    slug: String
    title: String
    description: String
    filename: String
    price: String
    photos: [String]
    shop: Shop
}

type Query {
    products: [Product]
    product(slug: String): [Product]
    productsFindHome: [Product]
}